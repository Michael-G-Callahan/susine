% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ERSS.R
\name{ERSS}
\alias{ERSS}
\title{Expected Residual Sum of Squares (ERSS)}
\usage{
ERSS(X, y, b_hat, b_2_hat)
}
\arguments{
\item{X}{Design matrix (n x p), standardized via \code{initialize_X} with
attributes \code{"d"} (rowSums of standardized X^2), \code{"scaled:center"}, and
\code{"scaled:scale"} present.}

\item{y}{Response vector of length n.}

\item{b_hat}{L x p matrix of per-effect posterior mean coefficients.}

\item{b_2_hat}{L x p matrix of per-effect posterior second moments
\eqn{E[b^2]} (variance + mean^2).}
}
\value{
Numeric scalar ERSS = \eqn{E[||y - X sum_l b_l||^2]}.
}
\description{
Expected Residual Sum of Squares (ERSS)
}
\examples{
# Simulate a small standardized design and response
set.seed(1)
n <- 100
p <- 20
X <- initialize_X(matrix(rnorm(n * p), nrow = n, ncol = p))
y <- as.numeric(X[, 1] - X[, 2] + rnorm(n))
L <- 5
b_hat <- matrix(rnorm(L * ncol(X)), nrow = L)
b_2_hat <- b_hat^2 + 1  # Example second moments
erss <- ERSS(X, y, b_hat, b_2_hat)
}
