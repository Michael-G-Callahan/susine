% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/susine_ss.R
\name{susine_ss}
\alias{susine_ss}
\title{SuSiNE with Summary Statistics}
\usage{
susine_ss(
  L,
  XtX,
  Xty,
  yty,
  n,
  mu_0 = NULL,
  sigma_0_2 = NULL,
  prior_inclusion_weights = NULL,
  residual_variance_lowerbound = 1e-04,
  prior_update_method = c("none", "var", "clamped_scale_var"),
  verbose = FALSE,
  convergence_method = c("elbo", "alpha"),
  tol = 1e-05,
  max_iter = 100,
  anneal_start_T = 1,
  anneal_schedule_type = c("geometric", "linear"),
  anneal_target = c("both", "likelihood", "prior"),
  anneal_burn_in = 0,
  prior_update_warmup = 0,
  init_random = FALSE,
  init_random_sd = 0.05,
  init_seed = NULL,
  init_alpha = NULL,
  init_effect_fits = NULL,
  auto_scale_mu_0 = FALSE,
  auto_scale_sigma_0_2 = FALSE,
  estimate_residual_variance = TRUE,
  verbose_beta_hat = FALSE,
  record_beta_hat_diagnostics = FALSE
)
}
\arguments{
\item{L}{Number of single effects (components).}

\item{XtX}{p x p cross-product matrix X^T X (original scale; standardized internally).}

\item{Xty}{p-vector X^T y (original scale; standardized internally).}

\item{yty}{Scalar y^T y.}

\item{n}{Sample size.}

\item{mu_0}{Prior effect means (scalar, length-L, length-p, or L x p).}

\item{sigma_0_2}{Prior effect variances as proportion of Var(y)
(scalar, length-L, length-p, or L x p).}

\item{prior_inclusion_weights}{Prior inclusion probabilities (length-L,
length-p, or L x p). If NULL, defaults to uniform 1/p.}

\item{residual_variance_lowerbound}{Lower bound for residual variance.}

\item{prior_update_method}{Empirical Bayes update strategy; one of
\code{"none"}, \code{"var"}, or \code{"clamped_scale_var"}.}

\item{verbose}{Logical; print iteration progress.}

\item{convergence_method}{Convergence criterion; one of \code{"elbo"} or
\code{"alpha"} (maximum absolute alpha change).}

\item{tol}{Convergence tolerance for the selected \code{convergence_method}.}

\item{max_iter}{Maximum number of iterations.}

\item{anneal_start_T}{Starting temperature (>= 1); 1 disables annealing.}

\item{anneal_schedule_type}{Schedule type: \code{"geometric"} or \code{"linear"}.}

\item{anneal_target}{Which variances to anneal: \code{"both"}, \code{"likelihood"}, or
\code{"prior"}.}

\item{anneal_burn_in}{Integer; number of initial iterations to anneal.}

\item{prior_update_warmup}{Integer; number of initial iterations to skip EB
updates (treat as \code{"none"}).}

\item{init_random}{If TRUE, randomize initial \code{b_hat} for each effect.}

\item{init_random_sd}{Standard deviation for randomized \code{b_hat} (standardized scale).}

\item{init_seed}{Optional integer seed for randomized initialization.}

\item{init_alpha}{Optional L x p (or conformable) matrix giving initial
posterior inclusion probabilities for each effect. If provided, these
alphas are used to initialize effect moments from the current priors and
residual variance.}

\item{init_effect_fits}{Optional list with conformable \code{alpha}, \code{b_hat}, and
\code{b_2_hat} matrices for exact warm-start initialization. Mutually exclusive
with \code{init_alpha}.}

\item{auto_scale_mu_0}{Logical; if TRUE, fit a global scaling c and noise
tau^2 for the prior mean annotations under the unconditional model and
apply c as \code{mu_0_scale_factor}.}

\item{auto_scale_sigma_0_2}{Logical; if TRUE, fit a global multiplicative
scale k for provided per-SNP prior variances via unconditional likelihood
m_j ~ N(mu_0j, shat2_j + k v0_j), and apply k as \code{sigma_0_2_scale_factor}.}

\item{estimate_residual_variance}{Logical; if FALSE, keep residual variance
fixed at its initial standardized-scale value.}

\item{verbose_beta_hat}{Logical; if TRUE, print per-iteration, per-effect
summaries of the current beta-hat distribution implied by the partial
residuals (\code{Xty / dXtX}) before each SER update.}

\item{record_beta_hat_diagnostics}{Logical; if TRUE, store the same
beta-hat summaries in \code{model_fit$beta_hat_diagnostics}.}
}
\value{
List with \code{priors}, \code{effect_fits}, and \code{model_fit} as in \code{susine} but
for the summary-statistics setting.
}
\description{
Fits SuSiNE using summary statistics (XtX, Xty, yty, n) instead
of raw X and y. Priors can be updated by empirical Bayes.
}
