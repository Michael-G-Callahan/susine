% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_fits.R
\name{aggregate_susine_fits}
\alias{aggregate_susine_fits}
\title{Aggregate SuSiNE fits across multiple runs}
\usage{
aggregate_susine_fits(fits, weighting = c("equal", "elbo"), temperature = 1)
}
\arguments{
\item{fits}{A list of fit objects returned by \code{susine()} or \code{susine_ss()}.}

\item{weighting}{One of \code{"equal"} (simple average) or \code{"elbo"} (evidence
weighted by exp(ELBO - max(ELBO))).}

\item{temperature}{Optional temperature (> 0) to soften/harden ELBO weights
when \code{weighting = "elbo"}. Higher values make weights more uniform.}
}
\value{
A list with a \code{model_fit} entry that mirrors the shape expected by
\code{report_metrics()}, containing at least:
\itemize{
\item \code{PIPs}: aggregated PIPs (length-p numeric vector)
\item \code{elbo}: length-1 numeric vector (aggregated ELBO)
\item \code{sigma_2}: length-1 numeric vector (aggregated residual variance)
Additionally includes \code{std_coef} if available from inputs.
}
}
\description{
Combines multiple \code{susine} (or \code{susine_ss}) fit objects into a
single lightweight aggregate suitable for downstream metrics like
\code{report_metrics()}. Aggregation is performed on the posterior inclusion
probabilities (PIPs) and basic diagnostics.
}
