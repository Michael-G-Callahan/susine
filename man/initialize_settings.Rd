% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize.R
\name{initialize_settings}
\alias{initialize_settings}
\title{Initialize user settings (fixed for model run)}
\usage{
initialize_settings(
  L,
  prior_update_method,
  tol,
  max_iter,
  verbose,
  residual_variance_lowerbound,
  convergence_method = c("elbo", "alpha"),
  estimate_residual_variance = TRUE,
  init_random = FALSE,
  init_random_sd = 0.05,
  init_seed = NULL,
  anneal_start_T = 1,
  anneal_schedule_type = c("geometric", "linear"),
  anneal_target = c("both", "likelihood", "prior"),
  anneal_burn_in = 0,
  prior_update_warmup = 0,
  c_nonneg = FALSE
)
}
\arguments{
\item{L}{Number of single effects (components).}

\item{prior_update_method}{Strategy for empirical Bayes updates; one of
\code{"none"}, \code{"var"}, or \code{"clamped_scale_var"}.}

\item{tol}{Convergence tolerance for the selected \code{convergence_method}.}

\item{max_iter}{Maximum number of coordinate ascent iterations.}

\item{verbose}{Logical; print ELBO progress.}

\item{residual_variance_lowerbound}{Lower bound for residual variance.}

\item{convergence_method}{Convergence criterion; one of \code{"elbo"} or \code{"alpha"}.}

\item{estimate_residual_variance}{Logical; if FALSE, keep residual variance
fixed at its initial value.}

\item{init_random}{If TRUE, randomize initial \code{b_hat} per effect.}

\item{init_random_sd}{Standard deviation for randomized \code{b_hat}.}

\item{init_seed}{Optional integer seed.}

\item{anneal_start_T}{Starting temperature (>= 1); 1 disables annealing.}

\item{anneal_schedule_type}{Temperature schedule type: "geometric" or "linear".}

\item{anneal_target}{Which variances to anneal: "both", "likelihood", or "prior".}

\item{anneal_burn_in}{Integer; first K iterations to anneal; after this, T=1.}

\item{prior_update_warmup}{Integer; first K iterations skip EB updates.}

\item{c_nonneg}{Logical; if TRUE, constrain the estimated annotation scale
factor \code{c} to be non-negative (\code{c >= 0}) during scale-based EB updates by
setting the optimizer lower bound to 0 instead of -100. Default FALSE.}
}
\value{
List of immutable settings used during the run.
}
\description{
Initialize user settings (fixed for model run)
}
