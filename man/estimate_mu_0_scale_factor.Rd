% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_priors.R
\name{estimate_mu_0_scale_factor}
\alias{estimate_mu_0_scale_factor}
\title{Estimate scaling factor for prior mean via Brent optimization (full data)}
\usage{
estimate_mu_0_scale_factor(
  beta_hat,
  shat2,
  mu_0_unscaled,
  log_tau2_lower = -30,
  log_tau2_upper = 15
)
}
\arguments{
\item{beta_hat}{Numeric vector of univariate regression coefficients \code{m_j}.}

\item{shat2}{Numeric vector of corresponding variances \code{v_j}.}

\item{mu_0_unscaled}{Numeric vector of unscaled prior means \code{a_j}.}

\item{log_tau2_lower}{Lower bound for \code{log(tau^2)} in Brent optimization.}

\item{log_tau2_upper}{Upper bound for \code{log(tau^2)} in Brent optimization.}
}
\value{
List with elements:
\itemize{
\item \code{c}: estimated scaling factor.
\item \code{tau2}: estimated extra variance component.
}
}
\description{
Estimates a scaling factor \code{c} for the prior mean \code{mu_0} by
maximizing the marginal log-likelihood of the unconditional regression
model: \code{m_j ~ N(c * a_j, v_j + tau^2)}, where \code{m_j} are univariate
regression coefficients (\code{beta_hat}), \code{a_j} are annotations (the unscaled
prior means \code{mu_0_unscaled}), \code{v_j} are their variances (\code{shat2}), and
\code{tau^2} is an extra variance component. The optimization is performed over
\code{tau^2} (on the log scale).
}
\keyword{internal}
