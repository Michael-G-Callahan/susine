% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize.R
\name{initialize_priors}
\alias{initialize_priors}
\title{Create prior distribution parameters structure}
\usage{
initialize_priors(
  mu_0,
  sigma_0_2,
  prior_inclusion_weights,
  p,
  L,
  var_y,
  X = NULL,
  y = NULL,
  Xty = NULL,
  dXtX = NULL,
  n = NULL,
  auto_scale_mu_0 = FALSE,
  auto_scale_sigma_0_2 = FALSE
)
}
\arguments{
\item{mu_0}{Prior effect means. Accepts scalar, length-L, length-p, or L x p
matrix; returned as L x p.}

\item{sigma_0_2}{Prior effect variances (as proportion of Var(y)). Accepts
scalar, length-L, length-p, or L x p; returned as L x p scaled by \code{var_y}.}

\item{prior_inclusion_weights}{Prior inclusion probabilities. Accepts length-L,
length-p, or L x p; returned as L x p with rows summing to ~1.}

\item{p}{Number of predictors (columns of X).}

\item{L}{Number of effects (components).}

\item{var_y}{Variance of y (used to scale \code{sigma_0_2}).}

\item{X}{Optional design matrix (standardized scale) used to compute
\code{beta_hat} and \code{shat2} if \code{auto_scale_mu_0=TRUE}.}

\item{y}{Optional response vector used with \code{X} for autoscaling.}

\item{Xty}{Optional p-vector \verb{X^T y} (standardized/original handled upstream)
used for autoscaling in summary-statistics workflows.}

\item{dXtX}{Optional p-vector \verb{diag(X^T X)} for autoscaling in SS workflows.}

\item{n}{Optional sample size (not strictly required for autoscaling).}

\item{auto_scale_mu_0}{Logical; if TRUE, estimate and store a global
\code{mu_0_scale_factor = c} and \code{mu_0_scale_tau2} using the unconditional model.}

\item{auto_scale_sigma_0_2}{Logical; if TRUE, estimate and store a global
multiplicative scale \code{sigma_0_2_scale_factor = k} for the provided per-SNP
prior variances using the unconditional model.}
}
\value{
Data.frame with L rows and columns:
\code{mu_0}, \code{sigma_0_2}, \code{prior_inclusion_weights} (each a length-p list-column),
\code{mu_0_scale_factor} (length-L vector), and \code{mu_0_scale_tau2} (length-L vector).
}
\description{
Create prior distribution parameters structure
}
